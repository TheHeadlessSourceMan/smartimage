def figureout(resultingImage):
	"""
	try to figure out what an effect did
	"""
	img=resultingImage
	radgrad,img=extractRadialGradient(img)
	vgrad,img=extractLinearGradient(img)
	noise,img=extractNoise(img)
	erosionAmt,img=extractErosion(img)
	curves,img=extractCurves(img)
	